const gatherResponses = []

getResponsesB = () => {
    if (this.state.message === "") {            
    } else {
        tokens.foreach(
            responseArray.foreach(
                if token in response.keywords {
                    gatherResponses.conca)
                }
            )
        )
    }
}


getResponsesC = () => {
    if (this.state.message === "") {            
    } else {
        const tokens = [...this.state.tokens]
        _.forEach(tokens, function(token) {
            console.log(token)
            
        });
    }
}